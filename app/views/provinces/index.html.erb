<br>
<table id="myTable" class="table">
  <thead>
    <tr>
      <th>Seleccionar Provincia: </th>
      <th>pertenece a Region: </th>
      <th>N° de Provincia: </th>
      <th>Opciones</th>

    </tr>
  </thead>

  <tbody>
    <% @provinces.each do |province| %>

      <tr>
        <td><%= province.name %></td>
        <td><%= province.id %></td>


        <td>
          <%= link_to 'Show', province %>
          <%= link_to 'Edit', edit_province_path(province) %>
          <%= link_to 'Destroy', province, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>

      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate(@courts, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer,previous_label: "Anterior", next_label: "Siguiente", class: 'pagination justify-content-center') %>

<br>

<div>
  <%= link_to 'Nueva Provincia', new_province_path %>
</div>

<p> </p>

<div>
  <%= link_to 'Ir a Region', regions_path %>
</div>
<p> </p>

<div>
  <%= link_to 'Ir a Comuna', communes_path %>
</div>
