
<script>
  $(document).ready(function() {
    $('select#province_region_id').select2({
      theme: "bootstrap"
    });
  });
</script>
<br>
<br>
<div class="row justify-content-md-center">
  <div class="card">
    <div class="card-body">
      <%= form_with model: @province, local: true do |form| %>
      <h3>Ingreso de nueva Provincia</h3>
      <div class="field">
        <%= form.label :name, "Nombre" %>
        <%= form.text_field :name, id: :name, autocomplete: "off" , class:"form-control border", required: true %>
      </div>
      <div class="form-group">
        <%= form.label :region_id, "Región" %>
        <%= form.select :region_id, options_for_select(Region.all.collect {|p| [p.name, p.id]}, { include_blank: "Seleccione"}) %>
      </div>
      <br>
      <div class="form-group">
        <%= form.submit "Guardar",class: "btn btn-success" %>
      </div>
      <% end %>
    </div>
  </div>
</div>
 
