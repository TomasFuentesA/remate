<%= form_with model: @persona_member, id: :formulary, remote: true do |form| %>


<%= form.label "persona jurídica" %>
<%= form.text_field :legal_persona_id, value: @legal_persona.id, class: 'form-control', readonly: true %>

<!--<%=  form.select :legal_persona_id , options_for_select(LegalPersona.order(:name).collect {|c| [c.name,c.id]}, @legal_persona.id), {include_blank: "Seleccione"} ,class:"custom-select mr-sm-2"%>-->


<%= form.label "persona natural" %>
<%=  form.select :persona_id , options_for_select(Persona.order(:name).collect {|c| [c.name,c.id]}), {include_blank: "Seleccione"} ,class:"custom-select mr-sm-2"%>

<%= form.label "porcentaje de participacion" %>
<%=  form.number_field :percentage %>


<%=  form.submit "guardar" %>

<% end %>
