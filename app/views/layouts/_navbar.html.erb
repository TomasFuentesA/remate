<% if current_user %>
<nav class="navbar navbar-expand-lg navbar-light " style="background-color: #ff850b;">
  
    <%=link_to "ALTITUD LTDA:::", dashboard_path,class:"navbar-brand" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <%if current_user %>
      <li class="nav-item ">
        <%=link_to "Remate", auctions_path,class:"nav-link" %> <span class="sr-only">(current)</span>
      </li>
      <li class="nav-item">
        <%=link_to "Propiedades", realties_path,class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%=link_to "Tribunales", courts_path,class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%=link_to "Personas", personas_path,class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%=link_to "Personas Jurídicas", legal_personas_path,class:"nav-link" %>
      </li>


      <% if can? :access, :admin_users %>
      <li class="nav-item">
        <%=link_to "Panel de administración", admin_dashboard_path,class:"nav-link" %>
      </li>
      <%end%>

      <%end%>
    </ul>


    <div class="navbar-nav ">
    <li class="nav-item dropdown ">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if user_signed_in? %>
            <% if current_user.name? %>
              Bienvenido, <%= current_user.name %>
            <% else %>
               Bienvenido
            <% end %>
          <% else %>
          Ingresar a cta
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <a class="dropdown-item" href="<%= destroy_user_session_path %> " data-method="DELETE">Salir</a>
          <% end %>
    </li>
    </div>
  </div>
</nav>
  <% end %>
