<h1>Inscriptions#index</h1>
<p>Find me in app/views/inscriptions/index.html.erb</p>


<table id="myTable" class="table">
  <thead>
    <tr>
      <th>Seleccionar inscription</th>

      <th>Nº inscription</th>
      <th>Opciones</th>
    </tr>
  </thead>




  <tbody>
    <% @inscriptions.each do |inscription| %>

      <tr>
        <td><%= inscription.name %></td>
        <td><%= inscription.number_inscription %></td>

        <td>
          <%= link_to 'Show', inscription %>
          <%= link_to 'Edit', edit_inscription_path(inscription) %>
          <%= link_to 'Destroy', inscription, method: :delete, data: { confirm: 'Are you sure?' } %>

      </tr>
    <% end %>
  </tbody>
</table>
